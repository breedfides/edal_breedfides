include ':eDAL-MetaDataAPI'
include ':eDAL-MetaDataAPI-Server'
include ':eDAL-MetaDataAPI-Client'

rootProject.name = 'electronicDataArchiveLibrary'

rootProject.children.each {
  it.buildFileName = it.name + '.gradle'
}